# Managed by Hydra

dataset_root_path: ${project_root_path}/data

voxel_size: 0.02
batch_size: 8
num_workers: 8
full_scale: [128, 512]
max_num_point: 250000

augmentation:
  jitter_xyz: True
  flip: True
  rotation: True
  elastic: False
  jitter_rgb: True
