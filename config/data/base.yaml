# Managed by Hydra

dataset_root_path: ${project_root_path}/data

file_suffix: .pth
ignore_label: -1
scale: 50
batch_size: 4
num_workers: 8
full_scale: [128, 512]
max_num_point: 250000

augmentation:
  jitter_xyz: True
  flip: True
  rotation: True
  elastic: True
  jitter_rgb: True
