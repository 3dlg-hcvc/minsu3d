# Managed by Hydra

defaults:
  - base

dataset: MultiScanObj
dataset_path: ${data.dataset_root_path}/multiscan_obj

metadata:
  metadata_path: ${data.dataset_path}/meta_data
  train_list: ${data.metadata.metadata_path}/obj_train.txt
  val_list: ${data.metadata.metadata_path}/obj_val.txt
  test_list: ${data.metadata.metadata_path}/obj_test.txt

classes: 20
ignore_classes: [0, 1, 2]
class_names: ['door', 'table', 'chair', 'cabinet', 'window', 'sofa', 'microwave', 'pillow',
              'tv_monitor', 'curtain', 'trash_can', 'suitcase', 'sink', 'backpack', 'bed', 'refrigerator','toilet']
